<?php

class EduerpTransactionServicesTestCase extends DrupalWebTestCase {
  
  protected $privileged_user;
  
  /**
   * getInfo() returns properties that are displayed in the test selection form.
   */
  public static function getInfo() {
    return array(
      'name' => 'EduerpTransactionServices module SimpleTest',
      'description' => 'Dummy tester to act as POST client and parser.',
      'group' => 'Eduerp',
    );
  }
  
  /**
   * setUp() performs any pre-requisite tasks that need to happen. 
   */
  public function setUp() {
    // Enable any modules required for the test.
    parent::setUp('uc_eduerp_fees, uc_cart, staff, uc_edupay, services');

    // Create and log in our privileged user.
    $this->privileged_user = $this->drupalCreateUser( array(
      'administer site configuration')
    );
    $this->drupalLogin($this->privileged_user);
  }
  
  /**
   * Example curl post:  
   * curl -v -X POST --data "uid=955&service=status&type= &status=payment_received&tpin=ac0e95f6cd994cc39807d986f7a10d4d&order_id=281" http://eduerp/service/fees/955/status/paid
   */ 
  public function testPayGatewayRequestHandler(){
    
  }
  
  //18eed6a382b40c4ede3d30315335c352
}